{% macro image( file, transform, default_alt_text, class ) -%}
	<img{% if class is not null %} class="{{ class }}"{% endif %} src="{{ file.getUrl( transform ) }}" width="{{ file.getWidth( transform ) }}" height="{{ file.getHeight( transform ) }}" alt="{{ file.alt ?? default_alt_text }}" loading="lazy">
{%- endmacro %}